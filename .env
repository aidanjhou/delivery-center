# Managed folders to scan (comma-separated)
# If missing or empty, will scan "packages" by default
MANAGED_FOLDERS=packages

# Server configuration
PORT=8080

# Database configuration
# SQLite database file for storing scanned distribution metadata
DATABASE_FILE=data.db
